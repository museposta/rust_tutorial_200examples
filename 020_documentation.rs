// three slashes use for documentation ///
// cargo doc --open  = generates documentation and opens.

/// color or clothes.
enum Color {
    Red,
    Green,
    Blue,
}

/// this struct hold emails of the customers.
struct Email{
    email: String,
}

/// function adds two numbers
fn add(a:i32, b:i32) -> i32 {
    a + b
}

/// the main function
fn main(){

}