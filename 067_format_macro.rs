

// format! writes the variable as string to a variable.

fn main() {
    let number = 10;
    let name = "Mustafa";
    let formatted_string = format!("The number is {} and my name is {}.", number, name);
    println!("{}", formatted_string);
  }