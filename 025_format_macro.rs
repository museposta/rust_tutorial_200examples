

// puts things to a string

fn main() {
    let name = "Alice";
    let age = 30;

    // Format the name and age.
    let formatted_string = format!("My name is {} and I am {} years old", name, age);

    // Print the formatted string.
    println!("{}", formatted_string);
}